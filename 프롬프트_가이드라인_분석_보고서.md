# 프롬프트 가이드라인 분석 보고서

## 🔍 문제 발견

### 발견된 문제: "출근" / "데일리룩" 반복 언급

**현상**: 사용자가 "출근"이나 "데일리"를 언급하지 않았는데도 챗봇이 자동으로 "출근용 스타일이나 데일리룩을 찾고 계시는군요!"라고 반복적으로 언급

**실제 대화 예시**:
- 사용자: "20대 남자 하의 추천 좀 해줘. 아우 요즘 덥다."
- 챗봇: "출근용 스타일이나 데일리룩을 찾고 계시는군요! 😊"

- 사용자: "30대 여자 옷 추천 좀"
- 챗봇: "어떤 용도로 필요하신지 궁금해요. 출근용인지, 아니면 데일리로 입고 싶으신 건가요?"

---

## 📋 코드 전수 조사 결과

### 1. 프롬프트 템플릿 하드코딩 문제

**위치**: `ml/response_generator/prompt_templates.py`

#### 문제점 1: `_style_mood_template()` (191번 줄)
```python
출근용인가요, 데일리인가요?
```
- **문제**: 사용자가 언급하지 않았는데도 고정된 질문으로 "출근용/데일리"를 언급
- **영향**: 모든 "감성·스타일" Intent에서 이 질문이 반복됨

#### 문제점 2: `_coordination_situation_template()` (328번 줄)
```python
{% if "출근" in user_message %}출근룩{% elif "데이트" in user_message %}데이트룩...%}을 찾고 계시는군요! 💼
```
- **상태**: 조건부 처리로 올바르게 구현됨 ✅
- **사용자가 "출근"을 언급한 경우에만 "출근룩" 사용**

---

### 2. 시스템 프롬프트 가이드라인

**위치**: `ml/response_generator/prompt_templates.py` - `_get_system_prompt()`

#### 현재 설정된 규칙:

```python
### 금지사항
- 같은 상품 반복 추천
- 같은 브랜드만 추천 (최대 2개)
- 비슷한 스타일만 추천
- 모호한 답변
- 3개 이상 연속 질문
```

#### ❌ 누락된 규칙:
- **사용자가 명시적으로 언급하지 않은 표현 사용 금지**
- **반복적인 표현 사용 금지**
- **추측 기반 표현 사용 금지**

---

### 3. 프롬프트 가이드라인 문서

**위치**: `Document/프롬프트 규칙 및 답변 작성 가이드_v1.0.md`

#### 문서의 예시 (242번 줄):
```
> 출근용인가요, 데일리인가요?
```
- **문제**: 이 예시가 템플릿에 그대로 반영되어 하드코딩됨
- **의도**: 예시일 뿐이지만, 코드에 고정 질문으로 구현됨

#### 문서의 금지사항 (71-79번 줄):
```
### ❌ 하지 말아야 할 것

|금지|설명|
|---|---|
|**같은 상품 반복 추천**|최근 추천한 상품 다시 추천 금지|
|**같은 브랜드만 추천**|특정 브랜드 3개 이상 연속 금지|
|**비슷한 스타일만 추천**|한 스타일에 편중 금지|
|**모호한 답변**|"상황에 따라 다릅니다"|
|**3개 이상 연속 질문**|"사이즈는요? 컬러는요? 예산은요?"|
```

**누락**: 반복 표현 금지 규칙이 없음

---

## 🔧 해결 방안

### 1. 시스템 프롬프트에 규칙 추가

**추가할 규칙**:
```
### 표현 사용 원칙
- 사용자가 명시적으로 언급한 표현만 사용
- 사용자가 언급하지 않은 상황/용도를 추측하여 언급하지 않음
- 반복적인 표현 사용 금지 (같은 표현을 여러 대화에서 반복 사용)
- 예시:
  ✅ 사용자가 "출근" 언급 → "출근룩을 찾고 계시는군요!"
  ❌ 사용자가 언급 안 함 → "출근용 스타일이나 데일리룩을 찾고 계시는군요!" (추측 금지)
```

### 2. 템플릿 수정

#### `_style_mood_template()` 수정 필요:

**현재 (191번 줄)**:
```python
출근용인가요, 데일리인가요?
```

**수정안 1: 조건부 질문**
```python
{% if "출근" in user_message or "데일리" in user_message or "데이트" in user_message or "여행" in user_message %}
{% if "출근" in user_message %}출근용으로 입으실 건가요?{% elif "데일리" in user_message %}데일리로 입으실 건가요?{% elif "데이트" in user_message %}데이트용인가요?{% elif "여행" in user_message %}여행용인가요?{% endif %}
{% else %}
어떤 상황에서 입으실 건가요?
{% endif %}
```

**수정안 2: 더 일반적인 질문**
```python
어떤 상황에서 입으실 건가요?
```

**수정안 3: 질문 제거 (추천 상품만 제공)**
```python
어떤 스타일이 더 마음에 드시나요?
```

### 3. 프롬프트 가이드라인 문서 업데이트

**추가할 섹션**:
```markdown
## 8. 표현 사용 원칙

### ✅ 사용자 언급 기반 표현
- 사용자가 명시적으로 언급한 키워드만 사용
- 예: 사용자가 "출근" 언급 → "출근룩" 사용 가능
- 예: 사용자가 언급 안 함 → "출근룩" 사용 금지

### ❌ 추측 기반 표현 금지
- 사용자가 언급하지 않은 상황/용도를 추측하여 언급하지 않음
- 예: "출근용인가요, 데일리인가요?" (사용자가 언급 안 한 경우)
- 예: "출근용 스타일이나 데일리룩을 찾고 계시는군요!" (추측 금지)

### 반복 표현 방지
- 같은 표현을 여러 대화에서 반복 사용하지 않음
- 매번 다른 방식으로 질문하거나 표현
```

---

## 📊 현재 프롬프트 가이드라인 요약

### ✅ 잘 설정된 부분

1. **필터버블 해소 원칙**: 시스템 프롬프트에 명시
2. **70/30 비율**: 명확히 정의됨
3. **Intent 분류**: 5가지 Intent 체계 정립
4. **응답 구조**: 4단계 구조 명확
5. **금지사항**: 기본적인 금지사항 정의

### ❌ 개선 필요한 부분

1. **반복 표현 금지 규칙 누락**
   - 시스템 프롬프트에 명시되지 않음
   - 프롬프트 가이드라인 문서에도 없음

2. **추측 기반 표현 금지 규칙 누락**
   - 사용자가 언급하지 않은 표현 사용 금지 규칙 없음

3. **템플릿 하드코딩 문제**
   - `_style_mood_template()`에 고정 질문 존재
   - 사용자 입력과 무관하게 "출근용/데일리" 언급

4. **표현 다양성 규칙 없음**
   - 같은 표현 반복 사용 방지 규칙 없음

---

## 🎯 권장 수정 사항

### 우선순위 1: 템플릿 수정
- `_style_mood_template()`의 고정 질문 제거 또는 조건부로 변경

### 우선순위 2: 시스템 프롬프트 업데이트
- 표현 사용 원칙 추가
- 추측 기반 표현 금지 규칙 추가

### 우선순위 3: 프롬프트 가이드라인 문서 업데이트
- 표현 사용 원칙 섹션 추가
- 예시 업데이트

---

## 📝 수정 코드 예시

### 시스템 프롬프트 수정

```python
def _get_system_prompt(self) -> str:
    """시스템 프롬프트 정의"""
    return """
당신은 패션 이커머스 플랫폼의 AI 스타일리스트입니다. 
사용자의 질문 의도를 파악하고, 새로운 발견이 있는 맥락 맞춤 상품을 추천합니다.

## 핵심 원칙: 필터버블 해소
"매번 비슷한 상품만 보여요" 문제를 해결하는 것이 최우선 목표입니다.

### 추천 구성 비율
- 새로운 발견: 70% (기존에 추천하지 않았던 상품, 새로운 스타일)
- 기존 취향: 30% (사용자가 좋아했던 스타일 기반)

### 표현 사용 원칙
- 사용자가 명시적으로 언급한 표현만 사용
- 사용자가 언급하지 않은 상황/용도를 추측하여 언급하지 않음
- 반복적인 표현 사용 금지 (같은 표현을 여러 대화에서 반복 사용)
- 예시:
  ✅ 사용자가 "출근" 언급 → "출근룩을 찾고 계시는군요!"
  ❌ 사용자가 언급 안 함 → "출근용 스타일이나 데일리룩을 찾고 계시는군요!" (추측 금지)

### 금지사항
- 같은 상품 반복 추천
- 같은 브랜드만 추천 (최대 2개)
- 비슷한 스타일만 추천
- 모호한 답변
- 3개 이상 연속 질문
- 사용자가 언급하지 않은 표현 사용 (추측 금지)
- 반복적인 표현 사용
"""
```

### 템플릿 수정

```python
def _style_mood_template(self) -> str:
    """감성·스타일 기반 템플릿"""
    return """
{% if "29CM" in user_message %}29CM 감성의{% elif "무신사" in user_message %}무신사 스타일의{% elif "미니멀" in user_message %}미니멀한{% elif "스트릿" in user_message %}스트릿{% endif %} 
{% if keywords %}{{ keywords | join(", ") }}{% endif %}을/를 찾고 계시는군요! ✨

{% if new_discoveries %}
💡 **새로운 발견**

평소 보시던 스타일 + 새로운 브랜드도 함께 추천드려요!

{% for item in new_discoveries %}
{{ loop.index }}. **{{ item.name }}**
   - {{ item.brand }} (새로운 브랜드)
   - {% if item.recommendation_type == "exploration" %}평소 스타일과는 살짝 다르지만, 요즘 트렌드예요!{% elif item.recommendation_type == "bridge" %}기존 취향 + 새로운 무드를 더해봤어요!{% endif %}
   - 비슷한 취향 고객 {{ 30 + (loop.index * 15) }}%가 만족한 제품!
{% endfor %}
{% endif %}

{% if existing_preferences %}
🏠 **내 취향**

{% for item in existing_preferences %}
{{ loop.index + (new_discoveries|length) }}. **{{ item.name }}**
   - 이전에 좋아하셨던 스타일과 비슷해요
{% endfor %}
{% endif %}

{% if "출근" in user_message or "데일리" in user_message or "데이트" in user_message or "여행" in user_message or "회식" in user_message %}
{% if "출근" in user_message %}출근용으로 입으실 건가요?{% elif "데일리" in user_message %}데일리로 입으실 건가요?{% elif "데이트" in user_message %}데이트용인가요?{% elif "여행" in user_message %}여행용인가요?{% elif "회식" in user_message %}회식용인가요?{% endif %}
{% else %}
어떤 상황에서 입으실 건가요?
{% endif %}
"""
```

---

## 📌 결론

### 현재 상태
- 프롬프트 가이드라인은 기본적으로 잘 설정되어 있음
- 하지만 **반복 표현 금지** 및 **추측 기반 표현 금지** 규칙이 누락됨
- 템플릿에 하드코딩된 고정 질문이 문제

### 개선 필요
1. 시스템 프롬프트에 표현 사용 원칙 추가
2. 템플릿의 고정 질문을 조건부로 변경
3. 프롬프트 가이드라인 문서 업데이트

---

*분석 일시: 2026-01-02*

