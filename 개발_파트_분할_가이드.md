# 패션 AI 추천 시스템 개발 파트 분할 가이드

## 📋 개요

본 문서는 **4명의 개발자**가 협업하여 패션 AI 추천 시스템을 구축하기 위한 파트 분할 가이드를 제공합니다.

**참고 문서:**
- `Document/패션 AI 추천 시스템 기술설계명세서 v1.3.md`
- `Document/AI 기능 개선 기획서_v1.1.md`

---

## 🎯 파트 분할 원칙

1. **독립성**: 각 파트는 최대한 독립적으로 개발 가능
2. **명확한 책임**: 각 파트의 역할과 책임이 명확히 구분
3. **의존성 최소화**: 파트 간 의존성을 최소화하여 병렬 개발 가능
4. **기술 스택 고려**: 각 파트의 기술 스택과 전문성 고려

---

## 👥 파트 구성

### 파트 A: 프론트엔드 (쇼핑몰 UI)
**담당자**: Frontend Developer 1  
**기술 스택**: React.js 18, JavaScript, CSS3, Axios

### 파트 B: 백엔드 API (비즈니스 로직)
**담당자**: Backend Developer 1  
**기술 스택**: FastAPI, Python, SQLAlchemy, PostgreSQL/SQLite

### 파트 C: AI/ML 서비스
**담당자**: ML Engineer  
**기술 스택**: Python, OpenAI API, NumPy, SciPy, SQL

### 파트 D: 데이터베이스 + 관리자 대시보드 + 인프라
**담당자**: Full-stack Developer / DevOps  
**기술 스택**: PostgreSQL, React.js, Docker, Docker Compose

---

## 📦 파트 A: 프론트엔드 (쇼핑몰 UI)

### 🎯 책임 범위

쇼핑몰 웹사이트의 사용자 인터페이스 및 사용자 경험 구현

### ✅ 개발 항목

#### 1. 쇼핑몰 기본 UI 컴포넌트

**파일 구조:**
```
frontend/src/
├── components/
│   ├── Header.js              # 헤더 (로고, 검색바, 사용자 메뉴)
│   ├── SearchBar.js           # 검색바
│   ├── ProductList.js         # 상품 목록
│   ├── ProductCard.js         # 상품 카드
│   ├── ProductDetailPage.js   # 상품 상세 페이지
│   ├── CartModal.js           # 장바구니 모달
│   ├── FavoritesModal.js      # 좋아요 모달
│   ├── AuthModal.js           # 인증 모달 (로그인/회원가입)
│   ├── CheckoutPage.js        # 결제 페이지
│   ├── OrderCompletePage.js   # 주문 완료 페이지
│   └── MyPage.js              # 마이페이지
├── contexts/
│   ├── AuthContext.js         # 인증 컨텍스트
│   ├── CartContext.js         # 장바구니 컨텍스트
│   └── FavoritesContext.js   # 좋아요 컨텍스트
├── services/
│   ├── cartService.js         # 장바구니 API
│   ├── favoriteService.js     # 좋아요 API
│   ├── searchService.js       # 검색 API
│   └── adminService.js        # 관리자 API (일부)
└── App.js                     # 메인 앱 컴포넌트
```

#### 2. 라우팅 구조

```javascript
// App.js
<Router>
  <Routes>
    <Route path="/" element={<AppContent />} />
    <Route path="/product/:productId" element={<ProductDetailPage />} />
    <Route path="/checkout" element={<CheckoutPage />} />
    <Route path="/order-complete/:orderId" element={<OrderCompletePage />} />
    <Route path="/mypage" element={<MyPage />} />
  </Routes>
</Router>
```

#### 3. 상태 관리

- **Context API**: 인증, 장바구니, 좋아요 상태 관리
- **LocalStorage**: 사용자 데이터, 장바구니, 좋아요, 주문 내역 영속성
- **React State**: 컴포넌트별 로컬 상태

#### 4. 주요 기능 구현

**4.1 상품 목록 및 검색**
- 상품 목록 조회 및 표시
- 카테고리별 필터링
- 검색 기능 (일반 검색 + 사용자 취향 기반 맞춤 검색)
- 무한 스크롤 또는 페이지네이션

**4.2 상품 상세 페이지**
- 상품 정보 표시 (이름, 브랜드, 가격, 이미지, 설명)
- 좋아요 버튼
- 장바구니 추가 버튼
- 구매하기 버튼

**4.3 장바구니 기능**
- 장바구니 모달 UI
- 상품 추가/삭제/수량 변경
- 장바구니 총액 계산
- LocalStorage 동기화

**4.4 좋아요 기능**
- 좋아요 버튼 UI
- 좋아요 목록 모달
- LocalStorage 동기화

**4.5 인증 기능**
- 로그인 모달
- 회원가입 모달
- 로그아웃 기능
- LocalStorage 기반 세션 관리

**4.6 주문/결제 기능**
- 결제 페이지 UI
- 주문 정보 입력 폼
- 주문 완료 페이지
- 주문 내역 조회 (마이페이지)

**4.7 마이페이지**
- 프로필 정보 표시
- 주문 내역 조회
- 좋아요 목록 조회
- 프로필 수정 기능

### 🔌 API 연동

**필요한 API 엔드포인트:**
- `GET /api/v1/products` - 상품 목록 조회
- `GET /api/v1/products/{product_id}` - 상품 상세 조회
- `GET /api/v1/recommendations/products/search` - 맞춤 검색
- `POST /api/v1/cart/add` - 장바구니 추가
- `GET /api/v1/cart/user/{user_id}` - 장바구니 조회
- `POST /api/v1/favorites/toggle` - 좋아요 토글
- `GET /api/v1/favorites/user/{user_id}` - 좋아요 목록
- `POST /api/v1/users/profile/{user_id}/initialize` - 프로필 초기화
- `GET /api/v1/users/profile/{user_id}` - 프로필 조회

### 📝 체크리스트

- [ ] Header 컴포넌트 구현
- [ ] ProductList 컴포넌트 구현
- [ ] ProductCard 컴포넌트 구현
- [ ] ProductDetailPage 구현
- [ ] CartModal 구현
- [ ] FavoritesModal 구현
- [ ] AuthModal 구현
- [ ] CheckoutPage 구현
- [ ] OrderCompletePage 구현
- [ ] MyPage 구현
- [ ] Context API 구현 (Auth, Cart, Favorites)
- [ ] LocalStorage 연동
- [ ] API 서비스 레이어 구현
- [ ] 라우팅 설정
- [ ] 반응형 디자인 적용
- [ ] 에러 핸들링
- [ ] 로딩 상태 처리

### 🎨 UI/UX 요구사항

- 모던하고 깔끔한 디자인
- 반응형 레이아웃 (모바일/태블릿/데스크톱)
- 부드러운 애니메이션
- 접근성 고려 (키보드 네비게이션, 스크린 리더 지원)

---

## 📦 파트 B: 백엔드 API (비즈니스 로직)

### 🎯 책임 범위

쇼핑몰의 비즈니스 로직 및 API 엔드포인트 구현

### ✅ 개발 항목

#### 1. API 엔드포인트 구현

**파일 구조:**
```
backend/app/
├── api/
│   └── api_v1/
│       ├── api.py
│       └── endpoints/
│           ├── health.py          # 헬스 체크
│           ├── users.py           # 사용자 API
│           ├── favorites.py       # 좋아요 API
│           ├── cart.py            # 장바구니 API
│           └── admin.py           # 관리자 API (일부)
├── services/
│   ├── user_service.py            # 사용자 서비스
│   ├── favorite_service.py        # 좋아요 서비스
│   ├── cart_service.py            # 장바구니 서비스
│   └── recommendation_service.py  # 추천 서비스 (일부)
├── models/
│   └── db_models.py               # 데이터베이스 모델
└── database.py                    # 데이터베이스 연결
```

#### 2. 데이터베이스 모델

**주요 테이블:**
- `users` - 사용자 정보
- `products` - 상품 정보
- `user_favorites` - 좋아요 목록
- `user_cart` - 장바구니
- `orders` - 주문 정보
- `order_items` - 주문 상품

#### 3. 주요 API 엔드포인트

**3.1 헬스 체크**
- `GET /api/v1/health/` - 기본 헬스 체크
- `GET /api/v1/health/detailed` - 상세 헬스 체크

**3.2 사용자 API**
- `GET /api/v1/users/profile/{user_id}` - 프로필 조회
- `PUT /api/v1/users/profile/{user_id}` - 프로필 수정
- `POST /api/v1/users/profile/{user_id}/initialize` - 프로필 초기화
- `GET /api/v1/users/preferences/{user_id}` - 선호도 조회
- `GET /api/v1/users/behavior/{user_id}` - 행동 분석

**3.3 좋아요 API**
- `POST /api/v1/favorites/add` - 좋아요 추가
- `DELETE /api/v1/favorites/remove` - 좋아요 삭제
- `POST /api/v1/favorites/toggle` - 좋아요 토글
- `GET /api/v1/favorites/user/{user_id}` - 사용자 좋아요 목록
- `GET /api/v1/favorites/user/{user_id}/product-ids` - 상품 ID 목록
- `GET /api/v1/favorites/check/{user_id}/{product_id}` - 좋아요 확인

**3.4 장바구니 API**
- `POST /api/v1/cart/add` - 장바구니 추가
- `PUT /api/v1/cart/update` - 장바구니 수정
- `DELETE /api/v1/cart/remove` - 장바구니 삭제
- `DELETE /api/v1/cart/clear/{user_id}` - 장바구니 비우기
- `GET /api/v1/cart/user/{user_id}` - 사용자 장바구니 조회

**3.5 상품 API (기본)**
- `GET /api/v1/products` - 상품 목록 조회
- `GET /api/v1/products/{product_id}` - 상품 상세 조회

**3.6 추천 API (기본)**
- `GET /api/v1/recommendations/products/search` - 맞춤 검색
  - 사용자 취향 기반 검색 결과 재정렬
  - 스타일 유사도 (가중치 0.4)
  - 카테고리 유사도 (가중치 0.3)
  - 브랜드 유사도 (가중치 0.2)
  - 가격 범위 유사도 (가중치 0.1)
  - 인기도 보너스 (최대 0.2)

**3.7 관리자 API (일부)**
- `GET /api/v1/admin/favorites/all` - 전체 좋아요 목록
- `GET /api/v1/admin/cart/all` - 전체 장바구니 목록

#### 4. 서비스 레이어 구현

**4.1 UserService**
- 사용자 프로필 조회/수정
- 사용자 선호도 분석
- 사용자 행동 분석

**4.2 FavoriteService**
- 좋아요 추가/삭제/토글
- 사용자별 좋아요 목록 조회
- 좋아요 통계

**4.3 CartService**
- 장바구니 추가/수정/삭제
- 장바구니 조회
- 장바구니 총액 계산

**4.4 RecommendationService (일부)**
- 사용자 취향 분석 (`analyze_user_preferences_from_db`)
  - 좋아요/장바구니 데이터 기반
  - 스타일 분포, 카테고리, 브랜드, 가격 범위 계산
- 맞춤 검색 결과 생성 (`get_personalized_search_results`)
  - 사용자 취향 반영한 검색 결과 재정렬

#### 5. 데이터베이스 스키마

**users 테이블:**
```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(255) UNIQUE NOT NULL,
    username VARCHAR(100) NOT NULL,
    password_hash VARCHAR(255),
    role VARCHAR(20) DEFAULT 'user',  -- 'admin' or 'user'
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL
);
```

**user_favorites 테이블:**
```sql
CREATE TABLE user_favorites (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(255) NOT NULL,
    product_id VARCHAR(255) NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UNIQUE(user_id, product_id)
);
```

**user_cart 테이블:**
```sql
CREATE TABLE user_cart (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(255) NOT NULL,
    product_id VARCHAR(255) NOT NULL,
    quantity INTEGER DEFAULT 1 NOT NULL,
    product_data JSONB,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
    UNIQUE(user_id, product_id)
);
```

**orders 테이블:**
```sql
CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    order_id VARCHAR(255) UNIQUE NOT NULL,
    user_id VARCHAR(255) NOT NULL,
    total_amount INTEGER NOT NULL,
    status VARCHAR(50) DEFAULT '주문완료',
    shipping_address TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL
);
```

### 🔌 외부 의존성

- **파트 C (AI/ML)**: 사용자 취향 분석 결과 활용 (선택적)
- **파트 D (DB)**: 데이터베이스 스키마 및 마이그레이션

### 📝 체크리스트

- [ ] FastAPI 앱 초기 설정
- [ ] 데이터베이스 연결 설정
- [ ] SQLAlchemy 모델 정의
- [ ] 사용자 API 엔드포인트 구현
- [ ] 좋아요 API 엔드포인트 구현
- [ ] 장바구니 API 엔드포인트 구현
- [ ] 상품 API 엔드포인트 구현 (기본)
- [ ] 추천 API 엔드포인트 구현 (맞춤 검색)
- [ ] 서비스 레이어 구현
- [ ] 에러 핸들링
- [ ] 입력 검증 (Pydantic)
- [ ] 로깅 설정
- [ ] API 문서화 (Swagger/OpenAPI)

---

## 📦 파트 C: AI/ML 서비스

### 🎯 책임 범위

AI 챗봇의 핵심 기능: Intent 분석, 벡터 검색, MMR 알고리즘, 응답 생성

### ✅ 개발 항목

#### 1. Intent 분석 모듈

**파일 구조:**
```
ml/
├── intent_analyzer/
│   ├── intent_classifier.py      # 의도 분류기
│   └── few_shot_prompts.py       # Few-shot 프롬프트
```

**기능:**
- 사용자 메시지의 의도 분류
- 5가지 Intent 타입:
  - 가격·가성비
  - 감성·스타일
  - 시즌·날씨
  - 체형·핏
  - 코디·상황
- 탐색 의도 감지 (`exploration_intent`)
- Few-shot learning 기반 분류
- GPT-4o-mini API 사용

**주요 메서드:**
```python
class IntentClassifier:
    def classify_intent(
        self,
        user_message: str,
        chat_history: List[Dict],
        user_profile: Optional[Dict] = None
    ) -> IntentResult:
        """
        사용자 메시지의 의도를 분류
        - Few-shot learning 기반
        - GPT-4o-mini API 사용
        """
```

#### 2. 벡터 검색 모듈

**파일 구조:**
```
ml/
├── vector_search/
│   ├── embedding_service.py      # 임베딩 생성
│   ├── search_engine.py          # 검색 엔진
│   ├── sql_vector_store.py       # SQL 벡터 스토어
│   └── vector_store.py          # 벡터 스토어 인터페이스
```

**기능:**
- 텍스트 임베딩 생성 (OpenAI text-embedding-3-large)
- SQL 기반 벡터 검색 (PostgreSQL/SQLite JSONB)
- 코사인 유사도 계산 (NumPy)
- 필터 적용 (카테고리, 성별 등)

**주요 메서드:**
```python
class SQLVectorStore:
    async def search_similar(
        self,
        query_embedding: np.ndarray,
        limit: int = 50,
        filters: Optional[Dict] = None
    ) -> List[Dict]:
        """
        유사 상품 검색
        - SQL 쿼리로 임베딩 조회
        - NumPy로 코사인 유사도 계산
        - 필터 적용 (카테고리, 성별 등)
        """
```

#### 3. MMR 알고리즘 모듈

**파일 구조:**
```
ml/
├── mmr_algorithm/
│   ├── mmr_scorer.py             # MMR 알고리즘
│   └── diversity_calculator.py   # 다양성 계산
```

**기능:**
- 유사도와 다양성의 균형 보장
- MMR 점수 계산
- 브랜드 다양성 체크 (동일 브랜드 최대 2개)
- 스타일 다양성 강제 (최소 3개 다른 스타일)
- 재추천 방지 (최근 5회 대화 제외)

**MMR 수식:**
```
MMR = λ × Similarity(q, d) - (1-λ) × max Similarity(d, d')
                                        d'∈R

q: 쿼리 (사용자 취향)
d: 후보 상품
R: 이미 선택된 추천 결과
λ: 유사도/다양성 가중치 (기본 0.7)
```

**주요 메서드:**
```python
class MMRScorer:
    def select_recommendations(
        self,
        candidates: List[ProductCandidate],
        strategy: Dict[str, float],  # {"exploitation": 0.3, "exploration": 0.7}
        total_count: int = 5
    ) -> List[ProductCandidate]:
        """
        필터버블을 해소한 추천 상품 리스트 생성
        - 신규 발견 70% + 기존 취향 30%
        - 재추천 방지 (최근 5회 대화 제외)
        - 브랜드 다양성 (동일 브랜드 최대 2개)
        - 스타일 다양성 (최소 3개 다른 스타일)
        """
```

#### 4. 응답 생성 모듈

**파일 구조:**
```
ml/
├── response_generator/
│   ├── response_generator.py      # 응답 생성기
│   └── prompt_templates.py       # 프롬프트 템플릿
```

**기능:**
- GPT-4o-mini 기반 자연어 응답 생성
- Jinja2 템플릿 기반 프롬프트 관리
- Intent별 템플릿 선택
- 4단계 응답 구조:
  1. 공감 + 의도 확인
  2. 새로운 발견 추천 (70%)
  3. 기존 취향 추천 (30%)
  4. 꼬리질문 or 대안 제시

**템플릿 종류:**
- `price_value` - 가격·가성비 기반
- `style_mood` - 감성·스타일 기반
- `season_weather` - 시즌·날씨 기반
- `fit_body` - 체형·핏 기반
- `coordination_situation` - 코디·상황 기반
- 각 Intent별 탐색 템플릿 (`_exploration`)

**주요 메서드:**
```python
class ResponseGenerator:
    async def generate_response(
        self,
        user_message: str,
        intent_result: IntentResult,
        recommendations: List[Dict],
        user_profile: Optional[Dict] = None,
        chat_history: Optional[List[Dict]] = None
    ) -> str:
        """
        AI 응답 생성
        - Intent 기반 템플릿 선택
        - 추천 상품 정보 포함
        - 자연어로 추천 이유 설명
        """
```

#### 5. 챗봇 서비스 통합

**파일 구조:**
```
backend/app/
├── services/
│   ├── chat_service.py           # 챗봇 서비스 (통합)
│   ├── chat_history_service.py   # 대화 이력 서비스
│   └── session_service.py        # 세션 서비스
├── api/
│   └── api_v1/
│       └── endpoints/
│           └── chat.py           # 챗봇 API 엔드포인트
```

**기능:**
- 사용자 메시지 수신 및 처리
- Intent 분석 호출
- 벡터 검색 실행
- MMR 알고리즘 적용
- 응답 생성
- 대화 이력 저장

**주요 API:**
- `POST /api/v1/chat/message` - 메시지 전송
- `GET /api/v1/chat/history/{session_id}` - 대화 이력 조회
- `GET /api/v1/chat/sessions/{user_id}` - 사용자 세션 목록
- `DELETE /api/v1/chat/session/{session_id}` - 세션 삭제

### 🔌 외부 의존성

- **OpenAI API**: GPT-4o-mini, text-embedding-3-large
- **파트 B (백엔드)**: 데이터베이스 접근 (상품, 사용자, 대화 이력)
- **파트 D (DB)**: 데이터베이스 스키마 (chat_sessions, chat_history, products)

### 📝 체크리스트

- [ ] Intent 분류기 구현
- [ ] Few-shot 프롬프트 작성
- [ ] 임베딩 서비스 구현
- [ ] SQL 벡터 스토어 구현
- [ ] 벡터 검색 엔진 구현
- [ ] MMR 알고리즘 구현
- [ ] 다양성 계산기 구현
- [ ] 프롬프트 템플릿 작성
- [ ] 응답 생성기 구현
- [ ] 챗봇 서비스 통합
- [ ] 챗봇 API 엔드포인트 구현
- [ ] 대화 이력 서비스 구현
- [ ] 세션 서비스 구현
- [ ] 에러 핸들링
- [ ] 로깅 설정
- [ ] 성능 최적화 (캐싱 등)

---

## 📦 파트 D: 데이터베이스 + 관리자 대시보드 + 인프라

### 🎯 책임 범위

데이터베이스 설계 및 관리, 관리자 대시보드, 인프라 구축

### ✅ 개발 항목

#### 1. 데이터베이스 설계 및 마이그레이션

**파일 구조:**
```
backend/
├── migrations/
│   └── init_db.sql            # 초기 스키마
├── models/
│   └── db_models.py           # SQLAlchemy 모델 (일부)
└── database.py                # 데이터베이스 연결
```

**주요 테이블:**

**1.1 상품 테이블 (products)**
```sql
CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    product_id VARCHAR(255) UNIQUE NOT NULL,
    name VARCHAR(255) NOT NULL,
    brand VARCHAR(100),
    category VARCHAR(50),
    gender VARCHAR(20),  -- '남성', '여성', '공용'
    style_tags JSONB,
    color VARCHAR(50),
    price INTEGER,
    popularity_score FLOAT DEFAULT 0.0,
    image_url TEXT,
    description TEXT,
    embedding JSONB,  -- 벡터 임베딩 배열 [0.12, 0.45, ...]
    metadata JSONB,   -- season, situation 등 추가 정보
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE INDEX idx_products_product_id ON products(product_id);
CREATE INDEX idx_products_category ON products(category);
CREATE INDEX idx_products_gender ON products(gender);
CREATE INDEX idx_products_brand ON products(brand);
CREATE INDEX idx_products_embedding ON products USING GIN (embedding);
```

**1.2 챗봇 세션 테이블 (chat_sessions)**
```sql
CREATE TABLE chat_sessions (
    id SERIAL PRIMARY KEY,
    session_id VARCHAR(255) UNIQUE NOT NULL,
    user_id VARCHAR(255) NOT NULL,
    is_active BOOLEAN DEFAULT true NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL,
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE INDEX idx_chat_sessions_session_id ON chat_sessions(session_id);
CREATE INDEX idx_chat_sessions_user_id ON chat_sessions(user_id);
```

**1.3 챗봇 이력 테이블 (chat_history)**
```sql
CREATE TABLE chat_history (
    id SERIAL PRIMARY KEY,
    session_id VARCHAR(255) NOT NULL,
    user_id VARCHAR(255) NOT NULL,
    role VARCHAR(20) NOT NULL,
    content TEXT NOT NULL,
    metadata JSONB,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE INDEX idx_chat_history_session_id ON chat_history(session_id);
CREATE INDEX idx_chat_history_user_id ON chat_history(user_id);
CREATE INDEX idx_chat_history_created_at ON chat_history(created_at);
```

**1.4 추천 이력 테이블 (recommendation_history)**
```sql
CREATE TABLE recommendation_history (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(255) NOT NULL,
    query TEXT NOT NULL,
    recommendations JSONB,
    total_count INTEGER NOT NULL,
    diversity_score FLOAT NOT NULL,
    strategy_used JSONB,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE INDEX idx_recommendation_history_user_id ON recommendation_history(user_id);
CREATE INDEX idx_recommendation_history_created_at ON recommendation_history(created_at);
```

**1.5 추천 피드백 테이블 (recommendation_feedback)**
```sql
CREATE TABLE recommendation_feedback (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(255) NOT NULL,
    product_id VARCHAR(255) NOT NULL,
    feedback_type VARCHAR(50) NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP NOT NULL
);

CREATE INDEX idx_recommendation_feedback_user_id ON recommendation_feedback(user_id);
CREATE INDEX idx_recommendation_feedback_product_id ON recommendation_feedback(product_id);
```

#### 2. 관리자 대시보드

**파일 구조:**
```
frontend/src/
├── components/
│   └── AdminDashboard.js      # 관리자 대시보드
├── services/
│   └── adminService.js        # 관리자 API
```

**기능:**

**2.1 상품 관리**
- 상품 목록 조회 (카드 그리드 레이아웃)
- 카테고리/성별 필터
- 상품 수정 모달
  - 기본 정보 수정 (이름, 브랜드, 카테고리, 가격, 이미지, 설명)
  - 스타일 태그 수정
  - 계절/상황 정보 수정
  - 임베딩 자동 재생성 버튼
- 상품 삭제
- 상품 추가 (선택적)

**2.2 사용자 관리**
- 사용자 목록 조회 (카드 그리드 레이아웃)
- 역할 필터 (전체/관리자/일반 사용자)
- 계정 추가 (ID, 이름, 비밀번호, 역할)
- 계정 편집 (이름, 역할)
- 계정 삭제

**2.3 주문 관리**
- 주문 목록 조회 (카드 그리드 레이아웃)
- 상태 필터 (전체/주문완료/결제완료/배송준비중/배송중/배송완료)
- 주문 상태 업데이트

**2.4 챗봇 대화 내역**
- 전체 대화 내역 조회
- 사용자별 대화 내역 조회
- 세션별 대화 내역 조회
- 대화 내역 검색/필터

**2.5 좋아요/장바구니 관리**
- 전체 좋아요 목록 조회
- 사용자별 좋아요 목록 조회
- 전체 장바구니 목록 조회
- 사용자별 장바구니 목록 조회

**2.6 챗봇 분석 대시보드**
- 전체 통계 (총 대화 수, 총 사용자 수 등)
- Intent 분포 차트
- 추천 성과 (CTR, 전환율)
- 시간대별 통계

#### 3. 관리자 API 엔드포인트

**파일 구조:**
```
backend/app/
├── api/
│   └── api_v1/
│       └── endpoints/
│           └── admin.py        # 관리자 API
```

**주요 API:**
- `GET /api/v1/admin/chat-history/all` - 전체 대화 내역
- `GET /api/v1/admin/chat-history/user/{user_id}` - 사용자별 대화 내역
- `GET /api/v1/admin/chat-history/session/{session_id}` - 세션별 대화 내역
- `GET /api/v1/admin/favorites/all` - 전체 좋아요 목록
- `GET /api/v1/admin/cart/all` - 전체 장바구니 목록
- `POST /api/v1/admin/products/init` - 상품 초기화
- `PUT /api/v1/admin/products/{product_id}` - 상품 수정
- `DELETE /api/v1/admin/products/{product_id}` - 상품 삭제
- `GET /api/v1/admin/users` - 사용자 목록
- `POST /api/v1/admin/users` - 사용자 추가
- `PUT /api/v1/admin/users/{user_id}` - 사용자 수정
- `DELETE /api/v1/admin/users/{user_id}` - 사용자 삭제
- `GET /api/v1/admin/orders` - 주문 목록
- `PUT /api/v1/admin/orders/{order_id}` - 주문 상태 업데이트
- `GET /api/v1/admin/analytics` - 분석 통계

#### 4. 인프라 구축

**파일 구조:**
```
프로젝트 루트/
├── docker-compose.yml           # Docker Compose 설정
├── nginx/
│   ├── nginx.conf              # Nginx 설정
│   └── conf.d/
│       └── default.conf        # 기본 설정
├── backend/
│   └── Dockerfile              # Backend Dockerfile
└── frontend/
    └── Dockerfile              # Frontend Dockerfile
```

**4.1 Docker Compose 설정**
```yaml
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:password@postgres:5432/fashion_ai_db
      - REDIS_URL=redis://redis:6379
    depends_on:
      - postgres
      - redis
    volumes:
      - ./backend:/app
      - ./ml:/ml

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:8000

  postgres:
    image: postgres:15-alpine
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
```

**4.2 데이터베이스 초기화 스크립트**
- `backend/migrations/init_db.sql` - 초기 스키마 생성
- `backend/scripts/init_products.py` - 더미 상품 데이터 생성

**4.3 환경 변수 설정**
- `.env` 파일 템플릿
- 환경별 설정 분리

#### 5. 상품 데이터 초기화

**파일 구조:**
```
backend/scripts/
├── init_products.py            # 상품 초기화
└── generate_products.py       # 더미 상품 생성
```

**기능:**
- 더미 상품 데이터 생성
- 상품 임베딩 생성
- 데이터베이스에 상품 데이터 삽입

### 🔌 외부 의존성

- **파트 A (프론트엔드)**: 관리자 대시보드 UI 연동
- **파트 B (백엔드)**: 관리자 API 엔드포인트 연동
- **파트 C (AI/ML)**: 상품 임베딩 생성 (선택적)

### 📝 체크리스트

- [ ] 데이터베이스 스키마 설계
- [ ] 마이그레이션 스크립트 작성
- [ ] Docker Compose 설정
- [ ] Nginx 설정
- [ ] Dockerfile 작성 (Backend, Frontend)
- [ ] 환경 변수 설정
- [ ] 관리자 대시보드 UI 구현
- [ ] 관리자 API 엔드포인트 구현
- [ ] 상품 관리 기능 구현
- [ ] 사용자 관리 기능 구현
- [ ] 주문 관리 기능 구현
- [ ] 챗봇 대화 내역 조회 기능 구현
- [ ] 챗봇 분석 대시보드 구현
- [ ] 더미 상품 데이터 생성 스크립트
- [ ] 상품 임베딩 생성 스크립트
- [ ] 데이터베이스 초기화 스크립트

---

## 🔄 파트 간 협업 가이드

### 1. API 인터페이스 정의

각 파트는 개발 시작 전에 **API 인터페이스를 먼저 정의**하고 공유해야 합니다.

**예시:**
```yaml
# 파트 B가 파트 A에게 제공하는 API
GET /api/v1/products
Response: {
  "products": [...],
  "total": 100
}

# 파트 C가 파트 B에게 제공하는 서비스
class RecommendationService:
    async def get_personalized_search_results(
        query: str,
        user_id: Optional[str] = None,
        limit: int = 10
    ) -> Dict[str, Any]
```

### 2. 데이터베이스 스키마 공유

**파트 D**는 개발 시작 전에 **데이터베이스 스키마를 먼저 정의**하고 공유해야 합니다.

**예시:**
```sql
-- 파트 D가 모든 파트에게 공유하는 스키마
CREATE TABLE products (...);
CREATE TABLE users (...);
```

### 3. 개발 순서 권장

1. **Week 1**: 파트 D - 데이터베이스 스키마 및 인프라 구축
2. **Week 2-3**: 파트 B - 백엔드 API 기본 구현
3. **Week 2-4**: 파트 C - AI/ML 모듈 구현
4. **Week 3-5**: 파트 A - 프론트엔드 UI 구현
5. **Week 5-6**: 파트 D - 관리자 대시보드 구현
6. **Week 6-7**: 통합 테스트 및 버그 수정

### 4. 통신 채널

- **일일 스탠드업**: 각 파트 진행 상황 공유
- **주간 회의**: API 인터페이스 검토 및 조정
- **슬랙/디스코드**: 실시간 소통

### 5. 코드 리뷰

- 각 파트는 다른 파트의 코드 리뷰에 참여
- 특히 API 인터페이스 변경 시 모든 파트에 공지

---

## 📊 개발 일정 (예시)

### Phase 1: 기반 구축 (Week 1-2)

| 파트 | 작업 | 기간 |
|------|------|------|
| D | 데이터베이스 스키마 설계 및 구축 | Week 1 |
| D | Docker Compose 인프라 구축 | Week 1 |
| B | FastAPI 기본 구조 및 데이터베이스 연결 | Week 1-2 |
| C | AI/ML 모듈 기본 구조 | Week 2 |

### Phase 2: 핵심 기능 개발 (Week 3-5)

| 파트 | 작업 | 기간 |
|------|------|------|
| B | 사용자/장바구니/좋아요 API 구현 | Week 3-4 |
| C | Intent 분석 모듈 구현 | Week 3 |
| C | 벡터 검색 모듈 구현 | Week 3-4 |
| C | MMR 알고리즘 구현 | Week 4 |
| C | 응답 생성 모듈 구현 | Week 4-5 |
| A | 쇼핑몰 기본 UI 구현 | Week 3-5 |
| A | 챗봇 위젯 UI 구현 (파트 C와 협업) | Week 5 |

### Phase 3: 통합 및 고도화 (Week 6-7)

| 파트 | 작업 | 기간 |
|------|------|------|
| B | 추천 API 통합 | Week 6 |
| C | 챗봇 서비스 통합 | Week 6 |
| D | 관리자 대시보드 구현 | Week 6-7 |
| 전체 | 통합 테스트 및 버그 수정 | Week 7 |

---

## 🎯 각 파트별 핵심 성공 지표

### 파트 A (프론트엔드)
- ✅ 모든 페이지 라우팅 정상 작동
- ✅ API 연동 성공률 100%
- ✅ 반응형 디자인 적용 완료
- ✅ 로딩/에러 상태 처리 완료

### 파트 B (백엔드)
- ✅ 모든 API 엔드포인트 정상 작동
- ✅ 데이터베이스 쿼리 최적화
- ✅ 에러 핸들링 완료
- ✅ API 문서화 완료

### 파트 C (AI/ML)
- ✅ Intent 분류 정확도 90% 이상
- ✅ 벡터 검색 응답 시간 1초 이내
- ✅ MMR 알고리즘 다양성 점수 0.5 이상
- ✅ 응답 생성 시간 3초 이내

### 파트 D (DB + 관리자 + 인프라)
- ✅ 데이터베이스 스키마 완성
- ✅ Docker Compose 정상 작동
- ✅ 관리자 대시보드 모든 기능 구현
- ✅ 더미 데이터 생성 완료

---

## 📝 체크리스트 (전체)

### 공통
- [ ] Git 저장소 설정 및 브랜치 전략 수립
- [ ] 코딩 컨벤션 정의
- [ ] API 문서화 도구 설정 (Swagger/OpenAPI)
- [ ] 로깅 시스템 구축
- [ ] 에러 핸들링 가이드라인 수립

### 파트별
- [ ] 파트 A: 프론트엔드 체크리스트 완료
- [ ] 파트 B: 백엔드 체크리스트 완료
- [ ] 파트 C: AI/ML 체크리스트 완료
- [ ] 파트 D: DB + 관리자 + 인프라 체크리스트 완료

### 통합
- [ ] 전체 시스템 통합 테스트
- [ ] 성능 테스트
- [ ] 보안 검토
- [ ] 문서화 완료

---

## 📚 참고 자료

### 기술 문서
- `Document/패션 AI 추천 시스템 기술설계명세서 v1.3.md`
- `Document/AI 기능 개선 기획서_v1.1.md`
- `프롬프트_가이드라인_분석_보고서.md`

### 코드 레퍼런스
- FastAPI 공식 문서: https://fastapi.tiangolo.com/
- React 공식 문서: https://react.dev/
- OpenAI API 문서: https://platform.openai.com/docs

---

**문서 버전**: 1.0  
**작성일**: 2026-01-02  
**대상**: 4명의 개발자 팀

